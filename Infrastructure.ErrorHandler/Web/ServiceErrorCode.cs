namespace Cmas.Infrastructure.ErrorHandler.Web
{
    public enum ServiceErrorCode
    {
        GeneralError = 0,
        NotFound = 10,
        InternalServerError = 20,
        InvalidToken = 30
    }
}
